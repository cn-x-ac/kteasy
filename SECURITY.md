# 安全策略

## 报告漏洞

请**不要**通过公开 Issue 报告安全问题，改用邮件：

- 收件：**master@x-ac.cn**（主题含 `Kteasy Security`）
- 响应承诺：**72 小时内**回复确认受理（人工核对，非自动回执）
- 如需加密通信，请在邮件里索要公钥；我们可以先约定一次性口令或改用其他渠道

## 处理流程

1. 确认复现路径与受影响版本；
2. 评估是否为**沙箱/权限/注入**类问题（本引擎的高风险面：EQL 参数化、表达式与脚本沙箱、开放接口签名与频控、
   外部表单免登录收集），这类问题按最高优先级处理；
3. 修复在私有分支进行，随下一个发布版本公开披露；
4. 披露时署名致谢（如你愿意）。

## 支持版本

| 版本线 | 状态 |
|---|---|
| `main` | 开发中，接受安全报告 |
| `0.x` | 尚未发布首个版本；`1.0.0` 起公布受支持版本表与安全更新窗口 |

## 你能为安全做的事

- 部署时始终经 HTTPS／反代访问，`/openapi/` 侧自行配置频控与 WAF；
- 数据库账号按最小权限授予（引擎需要 DDL 权限仅用于 `md`／`kteasy` 区与实体表物化，可按方言收窄）；
- 对象存储桶保持私有，附件一律经服务端签发访问；
- 关闭不需要的能力：脚本型自动化可用 `kteasy.automation.js.enabled=false` 整体停用（轻量部署形态）。
