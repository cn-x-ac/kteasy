# 防污染词表：命中即门禁失败（CI 用 grep -F 固定串匹配）。逐条一行，^# 开头为注释、被扫描命令剔除。
# 本词表文件自身由 --exclude 排除（词表必命中自己）；README/NOTICE 亦白名单（允许提及参照实现之名）。
# M0-03 起纳入裸 rebuild：源码/注释不得出现 "rebuild" 字样，引用旧实现请改写为"旧实现/参照实现"。
# 提交历史全量扫描见 M0-03 CI 步骤。
com.rebuild
rebuild
getrebuild
persist4j
AJQL
rb_
