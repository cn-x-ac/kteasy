# ---------------------------------------------------------------------------
# 单一版本来源：全仓依赖/插件版本只在此声明，模块 build.gradle.kts 内禁止出现版本号
# 版本取证（2026-09-18 实测 Maven Central / Gradle Plugin Portal / Spring Boot 官方文档）
#   - Spring Boot 4.1.1（4.x 线当前最新补丁）：官方 System Requirements = Java 17+（兼容至 26）、
#     Gradle 8.x（8.14 及以后）与 9.x、Spring Framework 7.0.9
#   - 其 BOM 锁定 kotlin.version=2.3.21、junit-jupiter.version=6.0.3、assertj.version=3.27.7
#   - Gradle wrapper 固定 8.14.5（见 gradle/wrapper/gradle-wrapper.properties）
# ---------------------------------------------------------------------------
[versions]
kotlin = "2.3.21"
springBoot = "4.1.1"
# 合规骨架：spotless 强制【规格】§1.4 版权头 + ktlint 统一 Kotlin 风格
spotless = "8.10.2"
ktlint = "1.8.0"
# 架构门禁（M1-02 红线④⑤）：把「方言只在 core.schema.dialect、业务层不触 SQL 字面量/具体方言类」变成 CI 断言
archunit = "1.4.0"

[libraries]
# --- build-logic 约定插件编译期需要的插件载体（模块内不引用） ---
kotlin-gradle-plugin = { module = "org.jetbrains.kotlin:kotlin-gradle-plugin", version.ref = "kotlin" }
kotlin-allopen-gradle-plugin = { module = "org.jetbrains.kotlin:kotlin-allopen", version.ref = "kotlin" }
kotlin-spring-gradle-plugin = { module = "org.jetbrains.kotlin.plugin.spring:org.jetbrains.kotlin.plugin.spring.gradle.plugin", version.ref = "kotlin" }
spring-boot-gradle-plugin = { module = "org.springframework.boot:spring-boot-gradle-plugin", version.ref = "springBoot" }
spotless-plugin-gradle = { module = "com.diffplug.spotless:spotless-plugin-gradle", version.ref = "spotless" }

# --- 运行时/测试依赖：不带版本，统一由 Spring Boot BOM 平台约束 ---
# Spring Boot 4 不再自动应用 io.spring.dependency-management，故一律用 platform(BOM) 对齐版本
spring-boot-dependencies = { module = "org.springframework.boot:spring-boot-dependencies", version.ref = "springBoot" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter" }
junit-platform-launcher = { module = "org.junit.platform:junit-platform-launcher" }
assertj-core = { module = "org.assertj:assertj-core" }
# kotlin-test 显式锁到与编译器同版（同源于 version.ref = kotlin），避免 BOM 与实际编译版本错位
kotlin-test = { module = "org.jetbrains.kotlin:kotlin-test", version.ref = "kotlin" }

# --- 服务端运行时依赖（M0-02 起）：全部由 BOM 平台约束版本，故不带 version.ref ---
# 口径核对（Boot 4.1.1 官方迁移指南）：Boot 4 起 spring-boot-starter-web 已改名 spring-boot-starter-webmvc；
# jdbc/validation 等 starter 名沿用；测试侧经典聚合器是 spring-boot-starter-test-classic 兜底件。
spring-boot-starter-webmvc = { module = "org.springframework.boot:spring-boot-starter-webmvc" }
spring-boot-starter-jdbc = { module = "org.springframework.boot:spring-boot-starter-jdbc" }
spring-boot-starter-validation = { module = "org.springframework.boot:spring-boot-starter-validation" }
# 传统 @SpringBootTest / MockMvc / AssertJ 聚合测试件（Boot 4 的 classic 兜底 starter）
spring-boot-starter-test-classic = { module = "org.springframework.boot:spring-boot-starter-test-classic" }
# 双库 JDBC 驱动（版本由 Boot BOM 统一管理；M0-02 仅装配连接，不写 SQL）
postgresql = { module = "org.postgresql:postgresql" }
mysql-connector-j = { module = "com.mysql:mysql-connector-j" }
# Flyway（数据库版本迁移工具，M0-03，只管引擎自身）：Boot 4 starter；
# Flyway 10 把 PostgreSQL 与 MySQL 的支持从核心模块拆成独立模块，两库都要各带一个。
spring-boot-starter-flyway = { module = "org.springframework.boot:spring-boot-starter-flyway" }
flyway-database-postgresql = { module = "org.flywaydb:flyway-database-postgresql" }
flyway-mysql = { module = "org.flywaydb:flyway-mysql" }
# 元数据图谱缓存（M1-01）：Caffeine 单飞加载；版本由 Boot BOM 平台统一管理
caffeine = { module = "com.github.ben-manes.caffeine:caffeine" }
# 架构门禁（M1-02）：ArchUnit 核心 API（不引 junit5 扩展，用普通 JUnit 测试直接 evaluate 规则）
archunit = { module = "com.tngtech.archunit:archunit", version.ref = "archunit" }

[bundles]
# 全模块通用的单元测试栈（L1 纯函数单测的地基）
unit-test = ["junit-jupiter", "assertj-core", "kotlin-test"]

[plugins]
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
kotlin-spring = { id = "org.jetbrains.kotlin.plugin.spring", version.ref = "kotlin" }
spring-boot = { id = "org.springframework.boot", version.ref = "springBoot" }
spotless = { id = "com.diffplug.spotless", version.ref = "spotless" }
