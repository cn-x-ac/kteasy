JSON_GIN_INDEX=ABSENT|无 JSON 整体索引；存在性/包含走多值索引或函数索引，大表存在性查询慢
JSON_MULTI_VALUED_INDEX=SUPPORTS|8.0.17+ CAST AS ARRAY 多值索引
ONLINE_INDEX_NO_LOCK=DEGRADED|INPLACE,LOCK=NONE，无 PG 式 CONCURRENTLY 窗口保证；失败降级 COPY
TRANSACTIONAL_DDL=ABSENT|DDL 隐式提交 → 物化作业强制分步断点续跑
VIRTUAL_COLUMN_INDEX=SUPPORTS|生成列 + 二级索引，成熟
INSTANT_ADD_COLUMN=SUPPORTS|8.0.12+ ALGORITHM=INSTANT（限表末尾、不可混操作）
SKIP_LOCKED=SUPPORTS|8.0 FOR UPDATE SKIP LOCKED
