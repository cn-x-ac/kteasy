JSON_GIN_INDEX=SUPPORTS|GIN + jsonb_path_ops 承载 @> 包含与存在性
JSON_MULTI_VALUED_INDEX=DEGRADED|PG 无 CAST AS ARRAY 多值索引；数组包含改由 GIN jsonb_path_ops 承载
ONLINE_INDEX_NO_LOCK=SUPPORTS|CREATE INDEX CONCURRENTLY（须事务外）
TRANSACTIONAL_DDL=SUPPORTS|DDL 可回滚
VIRTUAL_COLUMN_INDEX=SUPPORTS|PG18 VIRTUAL 生成列
INSTANT_ADD_COLUMN=SUPPORTS|加可空列瞬时
SKIP_LOCKED=SUPPORTS|FOR UPDATE SKIP LOCKED
